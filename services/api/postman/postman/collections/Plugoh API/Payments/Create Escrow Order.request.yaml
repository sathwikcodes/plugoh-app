$kind: http-request
url: "{{baseUrl}}/payment/create-escrow-order"
method: POST
headers:
  Authorization: Bearer {{businessJwt}}
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "campaign_id": "{{campaignId}}"
    }
order: 1000
