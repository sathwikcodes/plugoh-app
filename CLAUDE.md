@AGENTS.md

# Claude Code Notes For Plugoh

- `AGENTS.md` is the canonical repo instruction file; avoid duplicating those rules here.
- Use Claude-specific memory/rules only for local preferences or Claude-only behavior that should not affect other agents.
- If Claude stops following project guidance, verify that this import and the nearest nested `AGENTS.md` files are loaded before changing repo rules.
